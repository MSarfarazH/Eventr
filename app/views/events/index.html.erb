<%=link_to "Events By Category", interests_path%>

<h1>Upcoming Events:</h1> 
<ul>
<br>
<%@events.future_events.each do |event|%>
<li><%=link_to event.title, event_path(event)%> - <%=event.time_format%><br>
Users Also Attending: <%=event.num_attend%> </li>
<br>
<%end%>
</ul>

<br>
<h1>Past Events:</h1> 
<%@events.past_events.each do |event|%>
<%=link_to event.title, event_path(event)%> - <%=event.time_format_2%><br><br>
<%end%>

<br>
<br>
<br>
<hr>
<%=link_to "Register an Event", new_event_path%>
<br>
<br>
<hr>
 Logged In As: <%=link_to session[:username], user_path(session[:id])%> 
<br>
<%= button_to 'log out', {:controller => :sessions, :action => 'destroy'}, :method => :delete, class: "btn btn-primary"%>

